
// React JSX

const myProjects = <h1>On this webpage, you will find my projects.</h1>;

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(myProjects);

//Expressions with JSX

const myProportion = <h1> My job experiences is {2 * 2 / 2} times better with JSX</h1>;

    const myProjects = (
        <ul>
            <li> My Essay</li>
            <li> My Science Project</li>
            <li> My Spelling Bee</li>
        </ul>
    );

    // One Top Level Element
const gamesPlayed = (
    <div>
        <p>You have recently played the new game.</p>
        <p> The puzzle game has been recently installed and is ready to play.</p>
    </div>
);

const media = (
<div>
    <p> The current movie is ready to be streamed. </p>
<p> The show that you were interested in watching has been released. Stream or download now.</p>
</div>
)

const myVeggies = (
    <div>
    <li> Broccoli</li>
    <li> Kale </li>
    <li> Celery Sticks </li>
    </div>
)

const healthDesserts = (
    <div>
        <li> Apple</li>
        <li> Pear</li>
        <li>Orange</li>
    </div>
);

// Close elements
const myClassroom = <input type="Read today's history lesson." />

// Attribute class = className

const myGroup = <h1 className="dancers">We perform at our house and famous conventions.</h1>;

// You need 180 units to graduate
const u = 210;

const studentUnits = <h1>{(u) < 180 }? "You are eligible for graduation.": "You need more credits to graudate."</h1>;

    const myBook = ReactDOM.nametitle(h1,{} 'Explore my life'
    const root = ReactDOM.createRoot(document.getElementById(h1));
    root.render(myBook);

    